(function () {
    "use strict";







    // var app = WinJS.Application;
    // var activation = Windows.ApplicationModel.Activation;

    // var localSettings = Windows.Storage.ApplicationData.current.localSettings;
    // var applicationData = Windows.Storage.ApplicationData.current;
    //  var localSettings = applicationData.localSettings;


    /*
    WinJS.UI.Pages.define("/pages/settings/settings.html", {
        ready: function (element, options) {
            // Set the work weeks first
            setWorkWeeks();
            // Save new value
            document.getElementById("btnSaveSettings").addEventListener("click", saveSettings, false);
        }
    });
    */

    /*
    function setWorkWeeks() {
        var settingsWeeks = Windows.Storage.ApplicationData.current.localSettings.values["workWeeks"];
        // If settingsWeeks has no value, set default value to 48
        if (settingsWeeks === undefined) {
            settingsWeeks = "48";
        };
        workWeeks.value = settingsWeeks;
    }

    // Set "value" in workWeeks input to the current value
    $("#workWeeks").val(workWeeks);
    $("#workWeeksTitle").val(workWeeks);

    WinJS.Namespace.define("HomeFunctions", {
        test: function () {
            alert("Test");

        }
    });
    */




});